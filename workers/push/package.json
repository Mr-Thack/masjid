{
  "name": "@masjid/worker-push",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "echo 'no build step - wrangler compiles TS at deploy time'",
    "deploy": "wrangler deploy --env production",
    "deploy:dry": "wrangler deploy --env production --dry-run",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@masjid/schemas": "*",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
